# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# PostgreSQL Connection String (Example)
# Replace this with your actual connection string from Neon.tech, Supabase, or Render
DATABASE_URL="postgresql://user:password@host/database_name?sslmode=require"

# Session Secret (Change this to a random string for production)
SESSION_SECRET="change_me_to_a_long_random_string"

# Node Environment
NODE_ENV="production"
