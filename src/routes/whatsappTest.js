const express = require('express'); const router = express.Router(); const whatsappService = require('../services/whatsappService'); router.get('/test', async (req, res) => { const phone = req.query.phone; if(!phone) return res.json({error: 'no phone'}); const result = await whatsappService.sendMessage(phone, 'Test message dari system MMWO'); res.json({success: true, result}); }); module.exports = router;
