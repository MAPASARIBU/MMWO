const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const criticalParts = await prisma.part.findMany({ where: { is_active: true, wos: { none: { status: { notIn: ['CLOSED'] } } } }, include: { wos: true, legacy_wos: true } }); require('fs').writeFileSync('query_out.json', JSON.stringify(criticalParts, null, 2)); console.log('Done writing'); } run().catch(console.error).finally(() => prisma.$disconnect());
